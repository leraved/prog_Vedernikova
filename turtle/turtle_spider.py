import turtle

turtle.shape('turtle')

for x in range (12):
        turtle.forward(50)
        turtle.stamp()
        turtle.penup()
        turtle.left(180)
        turtle.forward(50)
        turtle.pendown()
        turtle.left(180)
        turtle.right(30)
